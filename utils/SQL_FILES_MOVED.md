# SQL Files Moved

The SQL files that were previously in this directory have been moved to `/db/migrations/` for better organization.

If you need to reference SQL scripts for database schema, migrations, or setup, please check that directory.

This change helps separate database-specific files from general utility functions.